# backend/requirements.txt
# 
# NOTE: If you encounter SSL certificate errors during installation, use:
# pip install --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org -r requirements.txt
#

# FastAPI and server - TESTED WORKING COMBINATION
# NOTE: FastAPI 0.104.1 + Pydantic 2.4.x-2.5.x are compatible
# DO NOT upgrade to FastAPI 0.118+ without testing compatibility
fastapi==0.104.1
starlette==0.27.0
pydantic>=2.4.0,<2.6.0
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database - MS SQL Express
sqlalchemy==2.0.23
alembic==1.12.1
pyodbc==5.0.1

# Authentication - Google OAuth + JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
authlib==1.3.0  # OAuth client library
httpx==0.27.0  # Async HTTP client for OAuth
itsdangerous==2.1.2  # Secure session management

# OpenAI API - Updated to latest stable version for compatibility
openai==1.54.0

# HTTP requests (for downloading images)
requests==2.31.0

# Google Cloud Services
google-cloud-storage==2.10.0
google-cloud-texttospeech==2.14.1
google-cloud-speech>=2.21.0  # Speech-to-Text for pronunciation analysis
google-generativeai>=0.3.0  # Gemini API for deep pronunciation coaching

# Language processing
epitran>=1.24  # Text-to-IPA conversion for pronunciation feedback

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Additional utilities
python-dotenv==1.0.0
pydantic-settings==2.1.0
packaging==24.0  # Required by OpenAI and other libraries

# Image handling (Phase 2)
# Pillow==10.1.0
# cloudinary==1.36.0